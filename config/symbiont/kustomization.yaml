---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Cluster prerequisites installed OUTSIDE the operator's namePrefix scope:
#   - the kube-symbiont-system namespace hosting the manager Deployment
#   - the symbiont-ballast PriorityClass referenced by every phantom pod
# Applied automatically by `make deploy`, or standalone:
#   kubectl apply -k config/symbiont
resources:
- bundle.yaml
